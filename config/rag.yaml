# 文本切割器配置
splitter:
  chunk_size: 500
  chunk_overlap: 50

# 向量数据库配置
vector_database:
  embedding_model_name: "text-embedding-v4"
  collection_name: "data"
  persist_directory: "./chroma_data"

# 相似度匹配配置
k: 3
fetch_k: 10